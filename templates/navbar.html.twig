<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('overview') }}">RunDiaries</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
        </ul>
        {% if is_granted('ROLE_ADMIN') %}
            <a class="btn btn-warning" href="{{ path('admin_dashboard') }}">Admin Dashboard</a>
        {% endif %}
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="p-1">
                <a class="btn btn-default" href="{{ path('show_user', { 'id': app.user.id }) }}">Profile</a>
                <a class="btn btn-danger" href="{{ path('logout') }}">Logout</a>
            </div>
        {% endif %}
        {% if (is_granted('IS_AUTHENTICATED_ANONYMOUSLY')) and (app.user == null) %}
            <div class="p-1">
                <a class="btn btn-warning" href="{{ path('register') }}">Register</a>
                <a class="btn btn-primary" href="{{ path('login') }}">Login</a>
            </div>
        {% endif %}
    </div>
</nav>
